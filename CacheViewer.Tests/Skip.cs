namespace CacheViewer.Tests
{
    internal static class Skip
    {
        internal const string LONG_RUNNING = "Long running";
        internal const string CREATES_FILES = "Creates files";
        internal const string CONNECTS_TO_DB = "Connects to SQL server";
    }
}